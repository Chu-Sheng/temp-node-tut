// npm - global command, conme with node
// npm --version

// local dependency - use it only in this particular project
// mpm i <packageName>

// global dependnency - use it in any project
// npm install -g <packageName>
// sudo install -g <packageName> (mac)